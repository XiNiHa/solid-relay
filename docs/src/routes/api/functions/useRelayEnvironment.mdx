# Function: useRelayEnvironment()

> **useRelayEnvironment**(): () => `Environment`

Defined in: index.d.mts:262

Returns the current Relay environment accessor from context.

Throws if called outside `RelayEnvironmentProvider`.

## Returns

> (): `Environment`

### Returns

`Environment`
